{{outlet}}
<div class="container">
  <br>
  {{new-project save="save"}}
  <div class="projectFeed">
    <ul>
      {{#each model.project as |project|}}
        {{project-tile project=project destroyProject="destroyProject"}}
      {{/each}}
    </ul>
  </div>
</div>
