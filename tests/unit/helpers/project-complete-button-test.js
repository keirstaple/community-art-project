import { projectCompleteButton } from 'community-art-project/helpers/project-complete-button';
import { module, test } from 'qunit';

module('Unit | Helper | project complete button');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = projectCompleteButton([42]);
  assert.ok(result);
});
